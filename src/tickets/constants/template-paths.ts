import * as path from 'path';

export const TICKET_TEMPLATE_PATH = path.resolve(
  __dirname,
  '../templates/ticket.ejs',
);

export const TICKET_LOGO_PATH = path.resolve(
  __dirname,
  '../assets/paradeisos-logo.svg',
);
